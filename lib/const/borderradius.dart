import 'package:flutter/material.dart';

class CustomRadius {
  static const BorderRadius btmRadius = BorderRadius.all(Radius.circular(40));
  static const BorderRadius cardRadius = BorderRadius.all(Radius.circular(16));
  static const BorderRadius imageRadius = BorderRadius.all(Radius.circular(10));
  static const BorderRadius tagRadius = BorderRadius.all(Radius.circular(3));
}
